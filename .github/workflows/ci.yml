name: CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build_and_test:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: clippy, rustfmt

    - name: Install cargo-nextest
      uses: taiki-e/install-action@nextest

    - name: Install dependencies (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake clang

    - name: Ensure workspace members exist
      run: |
        if [ ! -f crates/h265nal-test-ports/Cargo.toml ]; then
          mkdir -p crates/h265nal-test-ports/src
          cat > crates/h265nal-test-ports/Cargo.toml <<'EOF'
          [package]
          name = "h265nal-test-ports"
          version = "0.0.0"
          edition = "2021"
          publish = false

          [lib]
          path = "src/lib.rs"
          EOF
          cat > crates/h265nal-test-ports/src/lib.rs <<'EOF'
          pub fn workspace_placeholder() {}
          EOF
        fi

    - name: Check Rust formatting
      run: cargo fmt --all -- --check

    - name: Run clippy
      run: cargo clippy --workspace --all-targets -- -D warnings

    - name: Build
      run: cargo build

    - name: Run nextest
      run: cargo nextest run

    - name: Run doc tests
      run: cargo test --doc

    - name: Build CLI artifact (Release)
      run: cargo build --release -p h265nal-cli

    - name: Upload artifact (Linux)
      if: runner.os == 'Linux'
      uses: actions/upload-artifact@v4
      with:
        name: h265nal-cli-linux
        path: target/release/h265nal-cli
        if-no-files-found: error

    - name: Upload artifact (macOS)
      if: runner.os == 'macOS'
      uses: actions/upload-artifact@v4
      with:
        name: h265nal-cli-macos
        path: target/release/h265nal-cli
        if-no-files-found: error
