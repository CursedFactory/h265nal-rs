# Upstream C++ baseline repository (do not point this to local forks).
UPSTREAM_REPO_URL=https://github.com/chemag/h265nal

# Git ref to build from upstream baseline (branch, tag, or commit).
UPSTREAM_REF=main
